{
  "name": "@filecoin-pay/types",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript types generated from subgraph GraphQL schema",
  "main": "./dist/index.js",
  "type": "module",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist", "src"],
  "scripts": {
    "generate": "graphql-codegen --config codegen.yml",
    "build": "pnpm generate && tsc",
    "clean": "rm -rf dist generated",
    "lint": "biome check --write",
    "type-check": "pnpm generate && tsc --noEmit"
  },
  "dependencies": {
    "graphql": "^16.14.0"
  },
  "devDependencies": {
    "@filecoin-pay/configs": "workspace:*",
    "@graphql-codegen/cli": "^7.1.3",
    "@graphql-codegen/typescript": "^6.0.1",
    "@graphql-codegen/typescript-operations": "^6.0.5",
    "typescript": "^6.0.3"
  }
}
